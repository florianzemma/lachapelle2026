# Prismic Configuration
# Create a repository at https://prismic.io and replace with your repository name
NEXT_PUBLIC_PRISMIC_ENVIRONMENT=chapelle-tour-2026

# Optional: Prismic access token (only needed if repository is private)
# PRISMIC_ACCESS_TOKEN=your_access_token_here
